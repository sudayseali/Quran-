import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alquran.pro',
  appName: 'Al Quran Pro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
